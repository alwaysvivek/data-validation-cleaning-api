version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - APP_TITLE=Data Validation & Cleaning API (Docker)
    restart: always
